<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yanzhikai.testappexit">

    <uses-permission android:name="android.permission.RESTART_PACKAGES" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.A0Activity"
            android:launchMode="standard"/>
        <activity
            android:name=".Activities.B0Activity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".Activities.A1Activity"
            android:launchMode="standard" />
        <activity
            android:name=".Activities.B1Activity"
            android:launchMode="standard" />
        <activity
            android:name=".Activities.C0Activity"
            android:launchMode="standard" />
        <activity
            android:name=".Activities.C1Activity"
            android:launchMode="standard" />
    </application>

</manifest>